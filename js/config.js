/* ============================================================
   ABUGIDA CONFIGURATION
   ============================================================ */
const CONFIG = Object.freeze({
  API_BASE_URL: '/api',
  SESSION_CHECK_INTERVAL: 60000,
  OTP_RESEND_DELAY: 60,
  OTP_EXPIRY: 300,
  MAX_CHILDREN: 10,
  PASSWORD_MIN_LENGTH: 8,
  REMEMBER_ME_DAYS: 7,
  TOAST_DURATION: 4000,
  DEBOUNCE_DELAY: 300,
});
